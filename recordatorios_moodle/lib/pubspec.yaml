dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0

  # --- Paquetes faltantes que debes agregar ---
  workmanager: ^0.5.2 # Para las tareas en segundo plano
  flutter_local_notifications: ^17.1.2 # Para mostrar las notificaciones
  timezone: ^0.9.2 # Para el manejo de zonas horarias en notificaciones locales
  flutter_timezone: ^1.0.8 # Para obtener la zona horaria del dispositivo